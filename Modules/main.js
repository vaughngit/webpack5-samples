//const add = require("./functions/add")
//const {add, addAndMultiply, subtract} = require("./functions")
import {add, addAndMultiply, subtract} from './functions'

const output = add(1,2)
const output1 = addAndMultiply(1, 2, 3)

console.log(output)
console.log(output1)